{
  "name": "@consentos/banner",
  "version": "0.2.0",
  "private": true,
  "license": "Elastic-2.0",
  "description": "ConsentOS — consent banner loader and bundle",
  "scripts": {
    "build": "rollup -c",
    "build:watch": "rollup -c -w",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "@rollup/plugin-typescript": "^12.1.2",
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^26.0.0",
    "rollup": "^4.30.1",
    "tslib": "^2.8.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
